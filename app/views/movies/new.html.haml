%h1 Movies#new
%p Find me in app/views/movies/new.html.haml

= form_for @movie do |f|
  = f.label :title
  = f.text_field :title
  = f.label :release_year
  = f.select :release_year, (1900..2009).to_a.map(&:to_s)
  - @genres.each do |genre|
    = genre.name
    = check_box_tag "movie[genres][]", genre.id,@movie.genres.include?(genre), id: genre.name
  = f.submit "Save"

